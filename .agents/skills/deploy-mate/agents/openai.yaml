interface:
  display_name: Deploy Mate
  short_description: End-to-end deployment readiness for one environment — architecture, env vars, CI/CD artifacts, runtime visibility, secret injection, deploy.
policy:
  allow_implicit_invocation: false
