# Implementation Plans

Generated by the improve skill on 2026-08-20 against `829de43`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

OpenSpec mode: capability specs under `openspec/specs/` are canonical. Do not create PRDs, `PRODUCT.md`, or `DESIGN.md`. Plan 009 updates an existing spec requirement (import seam); plan 010 fills a TBD Purpose from existing requirements — neither introduces new product features.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Run `npm test` in CI validate workflow | P1 | S | — | DONE |
| 002  | Characterization tests for sync, import, static overlay, runUpdate | P1 | M | — | DONE |
| 003  | Fail closed on corrupt `.locks/upstream.json` | P1 | S | — | TODO |
| 004  | Drop unused prompt/simple-git deps; declare real `@inquirer/*` | P2 | S | — | TODO |
| 005  | Keep README category catalog in sync with manifests | P2 | S | — | TODO |
| 006  | Run git and npx via argv arrays, not shell strings | P1 | M | 002 | TODO |
| 007  | Confine sync/overlay relative paths inside intended roots | P1 | M | 002 | TODO |
| 008  | Clone each upstream repo once per sync | P2 | M | 002, 006 | TODO |
| 009  | Document tmp↔pipeline cycle; allow overlay-audit pending import | P2 | S | — | TODO |
| 010  | Fill overlay-pipeline spec Purpose from existing requirements | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **002 before 006/007/008** — mutation-path tests must exist so argv, path confinement, and clone-cache changes have a regression net. 002 does not require 001, but 001 should land first so CI actually runs the new tests once they exist.
- **006 before 008** — 008 changes clone/tmp cleanup around `syncSkill`; 006 already replaces `rm -rf` shell strings with `fs.rm`. Doing cache work on the old shell `rm` wastes a rewrite.
- **001, 003, 004, 005, 009, 010** are independent of each other and of 002.

## Findings considered and rejected

- Concurrent lock-file read-modify-write (`lib/locks.mjs` `updateLockEntry`): maintainer CLI is single-process in practice; file locking is not worth the NFS/deadlock surface.
- `overlay prepare` always applying static ops: matches overlay apply order (static before manifest assemble) in `openspec/specs/overlays`.
- CI shallow clone vs full blend validate: blend warnings are non-fatal per tooling spec; 001 adds tests rather than changing validate layers.
- Splitting `scripts/sync.mjs` / `lib/overlay-extract.mjs` by size alone: not worth the wiring risk.
- Per-file `git show` in extract-from-commit: no evidence extract is a bottleneck.
- Bulk import rewriting `skills.json` N times: import `--all` is rare vs sync.
- Unused `skills` npm package vs `npx skills add`: ambiguous pin; leave until install is changed on purpose.
- `scripts/sync.mjs` `.version('0.2.0')` vs package `0.5.0`: tiny DX; not selected.
- Concurrent extract partial writes: rare operator path.
- Empty `catch` in `validateBlendState` audit loop: not selected; leave unless it blocks 003.
- Tracked `Google Notebook/` tree: out of catalog, not selected this run.
