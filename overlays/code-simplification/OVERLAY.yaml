skill: code-simplification
description: User-invoked only — no model discovery

changes:
  - file: SKILL.md
    instructions: |
      Set `disable-model-invocation: true` in frontmatter. Keep `description` a one-line human summary; strip trigger lists.

      Preserve upstream simplification process, rules, and examples.
